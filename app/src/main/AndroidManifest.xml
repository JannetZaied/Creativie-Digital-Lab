<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cdlapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ActivityGallery"></activity>
        <activity android:name=".ui.ActivityContact" />
        <activity android:name=".ui.ActivityAboutUs" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.ActivityFormulaire" />

        <activity android:name=".ui.ActivityLogin"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>